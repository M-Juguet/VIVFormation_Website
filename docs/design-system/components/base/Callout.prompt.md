Encart qui sort une information de son flux : prérequis, confirmation, échéance de financement, report.

    <Callout tone="warning" title="Machine à vérifier">
      <p>8 Go de VRAM au minimum. Vérifiez la configuration avant la première journée.</p>
    </Callout>

**Tonalités** — `info` (défaut), `success`, `warning`, `danger`. Sans `title`, le titre est le nom de la tonalité : Information, Réussite, Attention, Erreur.

**À éviter** — Plus d'un encart par bloc de lecture. Un encart pour du texte courant.
